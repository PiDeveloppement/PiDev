<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ isEdit ? 'Modifier le partenariat' : 'Sponsoriser un evenement' }} - EventFlow</title>
    <link rel="stylesheet" href="{{ asset('css/sponsor.css') }}">
    <style>
        .portal-navbar {
            height: 80px;
            padding: 0 60px;
            border-bottom: 1px solid #e2e8f0;
            background: #ffffff;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .portal-logo-wrap img {
            width: auto;
            height: 56px;
            max-width: 180px;
            object-fit: contain;
            display: block;
        }

        .portal-nav-links {
            display: flex;
            gap: 40px;
            position: relative;
            top: -4px;
        }

        .portal-nav-link {
            font-size: 16px;
            font-weight: 500;
        }

        .portal-account-menu {
            position: relative;
            top: -4px;
            display: inline-flex;
            align-items: center;
        }

        .portal-account-menu summary {
            list-style: none;
            display: inline-flex;
            align-items: center;
            cursor: pointer;
        }

        .portal-account-menu summary::-webkit-details-marker {
            display: none;
        }

        .portal-account-chip {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            border: 1px solid #7fb2ea;
            background: linear-gradient(180deg, #3f8fe2 0%, #2d79ca 100%);
            border-radius: 20px;
            padding: 4px 10px 4px 6px;
            color: #ffffff;
            min-height: 42px;
        }

        .portal-account-avatar {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            background: #0f4d90;
            border: 2px solid rgba(255, 255, 255, 0.75);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: 800;
            color: #ffffff;
        }

        .portal-account-meta {
            display: flex;
            flex-direction: column;
            line-height: 1.1;
        }

        .portal-account-name {
            font-size: 14px;
            font-weight: 700;
            color: #ffffff;
        }

        .portal-account-role {
            font-size: 11px;
            color: rgba(255, 255, 255, 0.84);
        }

        .portal-account-dropdown {
            position: absolute;
            top: calc(100% + 8px);
            right: 0;
            min-width: 180px;
            background: #ffffff;
            border: 1px solid #d9e2ef;
            border-radius: 12px;
            box-shadow: 0 14px 28px rgba(15, 23, 42, 0.16);
            padding: 8px;
            z-index: 1200;
        }

        .portal-account-action {
            display: flex;
            align-items: center;
            gap: 8px;
            width: 100%;
            text-decoration: none;
            padding: 10px 12px;
            border-radius: 8px;
            color: #1e293b;
            font-weight: 600;
            font-size: 14px;
            border: none;
            background: transparent;
            text-align: left;
            cursor: pointer;
        }

        .portal-account-action:hover {
            background: #f3f7fd;
        }

        .nav-buttons {
            display: flex;
            gap: 15px;
            position: relative;
            top: -4px;
        }

        .btn-login {
            background: transparent;
            color: #0D47A1;
            font-weight: 700;
            border: 1.5px solid #0D47A1;
            border-radius: 8px;
            padding: 8px 24px;
            cursor: pointer;
            font-size: 14px;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .btn-login:hover {
            background: rgba(13, 71, 161, 0.05);
        }

        .btn-signup {
            background: #0D47A1;
            color: white;
            font-weight: 700;
            border: none;
            border-radius: 8px;
            padding: 8px 24px;
            cursor: pointer;
            font-size: 14px;
            box-shadow: 0 4px 8px rgba(13, 71, 161, 0.3);
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .btn-signup:hover {
            background: #0A3A80;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(13, 71, 161, 0.4);
        }

        .portal-translate {
            position: relative;
            top: -4px;
            margin-right: 12px;
        }

        .portal-translate-btn {
            border: none;
            border-radius: 999px;
            padding: 8px 14px;
            background: linear-gradient(135deg, #16a34a, #22c55e);
            color: #fff;
            font-size: 13px;
            font-weight: 700;
            cursor: pointer;
            box-shadow: 0 8px 18px rgba(22, 163, 74, 0.3);
        }

        .portal-translate-menu {
            position: absolute;
            top: calc(100% + 8px);
            right: 0;
            width: 180px;
            background: #fff;
            border: 1px solid #d9e2ef;
            border-radius: 12px;
            box-shadow: 0 14px 28px rgba(15, 23, 42, 0.16);
            padding: 8px;
            display: none;
            z-index: 1400;
        }

        .portal-translate-menu.open {
            display: block;
        }

        .portal-translate-opt {
            width: 100%;
            border: 1px solid #e2e8f0;
            background: #f8fafc;
            color: #0f172a;
            border-radius: 8px;
            padding: 8px 10px;
            text-align: left;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            margin-bottom: 6px;
        }

        .portal-translate-opt:last-child {
            margin-bottom: 0;
        }

        .portal-form-wrap {
            margin-top: 96px;
        }
    </style>
</head>
<body class="portal-form-body">
<nav class="portal-navbar">
    <div class="portal-logo-wrap">
        <img src="{{ asset('images/logo.png') }}" alt="EventFlow">
    </div>

    <div class="portal-nav-links">
        <a class="portal-nav-link" href="{{ path('app_landing') }}">Accueil</a>
        <a class="portal-nav-link" href="{{ path('app_public_events') }}">Evenements</a>
        <a class="portal-nav-link active" href="{{ path('app_sponsor_portal') }}">Recommandations sponsor</a>
        <a class="portal-nav-link" href="{{ path('app_landing') }}#features">Fonctionnalites</a>
        <a class="portal-nav-link" href="{{ path('app_feedback_view') }}">Feedback</a>
        <a class="portal-nav-link" href="{{ path('app_landing') }}#contact">Contact</a>
    </div>

    <div style="display:flex; align-items:center;">
        <div class="portal-translate" id="portalTranslateWidget">
            <button type="button" class="portal-translate-btn" id="portalTranslateToggle"><i class="fas fa-language"></i> Traduire</button>
            <div class="portal-translate-menu" id="portalTranslateMenu">
                <button type="button" class="portal-translate-opt" data-lang="fr">Francais</button>
                <button type="button" class="portal-translate-opt" data-lang="en">English</button>
                <button type="button" class="portal-translate-opt" data-lang="ar">Arabic</button>
                <button type="button" class="portal-translate-opt" data-lang="es">Espanol</button>
                <button type="button" class="portal-translate-opt" data-lang="de">Deutsch</button>
                <button type="button" class="portal-translate-opt" data-lang="it">Italiano</button>
            </div>
        </div>
        {% if app.user %}
            <details class="portal-account-menu">
                <summary class="portal-account-chip">
                    <span class="portal-account-avatar">{{ app.user.initials ?: (app.user.fullName ? app.user.fullName|slice(0, 1)|upper : 'C') }}</span>
                    <span class="portal-account-meta">
                        <span class="portal-account-name">{{ app.user.fullName ?: 'Compte' }}</span>
                        <span class="portal-account-role">Sponsor</span>
                    </span>
                </summary>
                <div class="portal-account-dropdown">
                    <button type="button" class="portal-account-action">Profile</button>
                    <a class="portal-account-action" href="{{ path('app_logout') }}">Deconnexion</a>
                </div>
            </details>
        {% else %}
            <div class="nav-buttons">
                <a class="btn-login" href="{{ path('app_login') }}">Connexion</a>
                <a class="btn-signup" href="{{ path('app_register') }}">Inscription</a>
            </div>
        {% endif %}
    </div>
</nav>

<div class="portal-form-wrap sponsor-page">
    <div class="flash-wrap">
        {% for message in app.flashes('success') %}<div class="flash-success">{{ message }}</div>{% endfor %}
        {% for message in app.flashes('error') %}<div class="flash-error">{{ message }}</div>{% endfor %}
    </div>

    <div class="portal-form-card">
        <div class="portal-form-header">
            <div class="title-wrap">
                <span class="portal-form-badge">{{ isEdit ? '✎' : '✚' }}</span>
                <div>
                    <h3>{{ isEdit ? 'Modifier le partenariat' : 'Nouveau partenariat' }}</h3>
                    <p>{{ isEdit ? 'Mettez a jour les informations de votre sponsoring' : 'Remplissez les informations de l entreprise partenaire' }}</p>
                </div>
            </div>
            <strong>EventFlow</strong>
        </div>

        <div class="portal-form-content">
            {{ form_start(form) }}

            <section class="portal-form-section blue">
                <h4 class="portal-form-section-title">📋 Informations générales</h4>
                <div class="portal-form-grid">
                    {% if event %}
                    <div>
                        <label>Événement</label>
                        <div class="form-control" style="background: #f5f8fc; padding: 10px 12px; border-radius: 6px; border: 1px solid #e2e8f0;">
                            <strong>{{ event.title }}</strong>
                        </div>
                    </div>
                    {% else %}
                    <div>
                        {{ form_label(form.eventId) }}
                        {{ form_widget(form.eventId) }}
                        <div class="form-errors">{{ form_errors(form.eventId) }}</div>
                    </div>
                    {% endif %}
                    <div>
                        {{ form_label(form.companyName) }}
                        {{ form_widget(form.companyName, {'attr': {'placeholder': 'Nom entreprise'}}) }}
                        <div class="form-errors">{{ form_errors(form.companyName) }}</div>
                    </div>
                    <div>
                        {{ form_label(form.contactEmail) }}
                        {{ form_widget(form.contactEmail, {'attr': {'placeholder': 'contact@entreprise.com'}}) }}
                        <div class="form-errors">{{ form_errors(form.contactEmail) }}</div>
                    </div>
                    <div>
                        {{ form_label(form.phone) }}
                        {{ form_widget(form.phone, {'attr': {'placeholder': '216XXXXXXXX'}}) }}
                        <div class="form-errors">{{ form_errors(form.phone) }}</div>
                    </div>
                    <div>
                        {{ form_label(form.industry) }}
                        {{ form_widget(form.industry, {'attr': {'placeholder': 'ex: Technologie, Marketing'}}) }}
                        <div class="form-errors">{{ form_errors(form.industry) }}</div>
                    </div>
                    <div>
                        {{ form_label(form.taxId) }}
                        {{ form_widget(form.taxId, {'attr': {'placeholder': 'ex: 1234567A'}}) }}
                        <div class="form-errors">{{ form_errors(form.taxId) }}</div>
                    </div>
                </div>
            </section>

            <section class="portal-form-section purple">
                <h4 class="portal-form-section-title">Logo de l entreprise</h4>
                <div class="logo-toolbar">
                    <button type="button" class="btn-sponsor light" id="pick-logo-btn">Choisir logo</button>
                    <button type="button" class="btn-sponsor danger" id="clear-logo-btn">Supprimer logo</button>
                    <div style="display:none;">
                        {{ form_widget(form.logoFile) }}
                    </div>
                    <span class="logo-file-name" id="logo-file-name">Aucun fichier</span>
                </div>
                <div class="form-errors">{{ form_errors(form.logoFile) }}</div>

                <div class="logo-layout">
                    <div class="logo-preview" id="logo-preview-box">
                        {% if form.logoUrl.vars.value %}
                            <img id="logo-preview-img" src="{{ form.logoUrl.vars.value }}" alt="Logo preview">
                        {% else %}
                            <img id="logo-preview-img" alt="Logo preview">
                        {% endif %}
                        <span class="placeholder" id="logo-preview-placeholder">Aperçu logo</span>
                    </div>

                    <div>
                        {{ form_label(form.logoUrl) }}
                        {{ form_widget(form.logoUrl, {'attr': {'placeholder': 'URL logo (auto)'}}) }}
                        <div class="form-errors">{{ form_errors(form.logoUrl) }}</div>
                        <div class="field-help">Vous pouvez coller une URL du logo ou choisir un fichier pour l apercu visuel.</div>
                    </div>
                </div>
            </section>

            <section class="portal-form-section green">
                <h4 class="portal-form-section-title">Contribution financiere</h4>
                <div class="finance-row">
                    <div>
                        {{ form_label(form.contributionName) }}
                        {{ form_widget(form.contributionName, {'attr': {'id': 'contribution-input', 'placeholder': 'ex: 3500'}}) }}
                        <div class="form-errors">{{ form_errors(form.contributionName) }}</div>
                    </div>
                    <div>
                        {{ form_label(form.contributionCurrency) }}
                        {{ form_widget(form.contributionCurrency, {'attr': {'id': 'currency-view'}}) }}
                        <div class="form-errors">{{ form_errors(form.contributionCurrency) }}</div>
                    </div>
                </div>
                <div class="finance-result">En TND: <span id="contribution-tnd">-</span></div>
            </section>

            <div class="portal-form-footer">
                <a class="btn-sponsor outline" href="{{ path(backRoute) }}">Annuler</a>
                <button class="btn-sponsor success" type="submit">{{ isEdit ? 'Enregistrer les modifications' : 'Enregistrer le partenariat' }}</button>
            </div>

            {{ form_end(form, {'render_rest': true}) }}
        </div>
    </div>
</div>

<script>
(function () {
    const logoUrlInput = document.getElementById('{{ form.logoUrl.vars.id }}');
    const contributionInput = document.getElementById('contribution-input');
    const currencyView = document.getElementById('currency-view');
    const contributionOut = document.getElementById('contribution-tnd');

    const pickLogoBtn = document.getElementById('pick-logo-btn');
    const clearLogoBtn = document.getElementById('clear-logo-btn');
    const logoFileInput = document.getElementById('{{ form.logoFile.vars.id }}');
    const logoFileName = document.getElementById('logo-file-name');
    const logoPreviewImg = document.getElementById('logo-preview-img');
    const logoPreviewPlaceholder = document.getElementById('logo-preview-placeholder');

    function parseAmount(value) {
        if (!value) return null;
        const normalized = String(value).replace(',', '.').trim();
        const n = parseFloat(normalized);
        return Number.isFinite(n) ? n : null;
    }

    async function updateContribution() {
        if (!contributionInput || !currencyView || !contributionOut) return;
        const amount = parseAmount(contributionInput.value);
        if (amount === null) {
            contributionOut.textContent = '-';
            return;
        }

        const currency = currencyView.value || 'TND';
        if (currency === 'TND') {
            contributionOut.textContent = amount.toLocaleString('fr-FR', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) + ' DT';
            return;
        }

        try {
            const url = `{{ path('app_currency_convert') }}?amount=${encodeURIComponent(String(amount))}&from=${encodeURIComponent(currency)}&to=TND`;
            const response = await fetch(url);
            const payload = await response.json();

            if (!response.ok || !payload.ok) {
                contributionOut.textContent = '-';
                return;
            }

            contributionOut.textContent = Number(payload.converted).toLocaleString('fr-FR', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) + ' DT';
        } catch (error) {
            contributionOut.textContent = '-';
        }
    }

    function setLogoPreview(src) {
        if (!logoPreviewImg || !logoPreviewPlaceholder) return;
        if (src && src.trim() !== '') {
            logoPreviewImg.src = src;
            logoPreviewImg.style.display = 'block';
            logoPreviewPlaceholder.style.display = 'none';
        } else {
            logoPreviewImg.removeAttribute('src');
            logoPreviewImg.style.display = 'none';
            logoPreviewPlaceholder.style.display = 'block';
        }
    }

    if (logoUrlInput) {
        logoUrlInput.addEventListener('input', function () {
            setLogoPreview(logoUrlInput.value);
        });
        setLogoPreview(logoUrlInput.value);
    }

    if (pickLogoBtn && logoFileInput) {
        pickLogoBtn.addEventListener('click', function () {
            logoFileInput.click();
        });

        logoFileInput.addEventListener('change', function () {
            const file = logoFileInput.files && logoFileInput.files[0] ? logoFileInput.files[0] : null;
            logoFileName.textContent = file ? file.name : 'Aucun fichier';

            if (!file) return;
            const reader = new FileReader();
            reader.onload = function (e) {
                setLogoPreview(String(e.target && e.target.result ? e.target.result : ''));
            };
            reader.readAsDataURL(file);
        });
    }

    if (clearLogoBtn) {
        clearLogoBtn.addEventListener('click', function () {
            if (logoFileInput) logoFileInput.value = '';
            if (logoUrlInput) logoUrlInput.value = '';
            if (logoFileName) logoFileName.textContent = 'Aucun fichier';
            setLogoPreview('');
        });
    }

    if (contributionInput) contributionInput.addEventListener('input', updateContribution);
    if (currencyView) currencyView.addEventListener('change', updateContribution);

    updateContribution();
})();
</script>
<div id="google_translate_element" style="display:none;"></div>
<script>
    (function () {
        const toggle = document.getElementById('portalTranslateToggle');
        const menu = document.getElementById('portalTranslateMenu');
        const widget = document.getElementById('portalTranslateWidget');

        const setTranslateCookie = function (value) {
            const host = window.location.hostname;
            document.cookie = 'googtrans=' + value + ';path=/';
            if (host && host !== 'localhost' && host.indexOf('.') !== -1) {
                document.cookie = 'googtrans=' + value + ';path=/;domain=.' + host;
            }
        };

        if (toggle && menu) {
            toggle.addEventListener('click', function () {
                menu.classList.toggle('open');
            });

            document.addEventListener('click', function (event) {
                if (!widget || widget.contains(event.target)) {
                    return;
                }
                menu.classList.remove('open');
            });

            menu.querySelectorAll('[data-lang]').forEach(function (button) {
                button.addEventListener('click', function () {
                    const lang = button.getAttribute('data-lang') || 'en';
                    setTranslateCookie(lang === 'fr' ? '/fr/fr' : '/fr/' + lang);
                    window.location.reload();
                });
            });
        }
    })();

    function googleTranslateElementInit() {
        if (!window.google || !window.google.translate || !window.google.translate.TranslateElement) {
            return;
        }

        new window.google.translate.TranslateElement({
            pageLanguage: 'fr',
            includedLanguages: 'fr,en,ar,es,de,it',
            autoDisplay: false,
            multilanguagePage: true
        }, 'google_translate_element');
    }
</script>
<script src="https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
</body>
</html>
