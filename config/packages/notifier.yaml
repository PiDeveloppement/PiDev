framework:
    notifier:
        chatter_transports: ~

        texter_transports:
            infobip: '%env(INFOBIP_DSN)%'
            twilio: '%env(TWILIO_DSN)%'
        channel_policy:
            urgent: ['sms']
            high: ['sms']
            medium: ['email']
            low: ['email']

        admin_recipients:
            - { email: admin@example.com }