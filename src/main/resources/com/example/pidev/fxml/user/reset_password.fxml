<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.pidev.controller.user.ResetPasswordController"
            style="-fx-background-color: linear-gradient(to bottom right, #f0f8ff, #e6f2ff);"
            prefHeight="600" prefWidth="800">

    <center>
        <VBox alignment="CENTER" spacing="25" maxWidth="500" maxHeight="600"
              style="-fx-background-color: white; -fx-background-radius: 15; -fx-padding: 40;
                     -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 10, 0, 0, 0);">

            <!-- Icône de clé -->
            <Label text="🔑" style="-fx-font-size: 60px;"/>

            <!-- Titre -->
            <Label text="Nouveau mot de passe"
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #2196F3;"/>

            <!-- Informations sur le token -->
            <Label fx:id="tokenInfoLabel"
                   text="Validation du lien..."
                   style="-fx-text-fill: #666; -fx-font-size: 13px;"
                   wrapText="true" textAlignment="CENTER"/>

            <!-- Indicateur de chargement -->
            <ProgressIndicator fx:id="loadingIndicator" visible="false" prefWidth="40" prefHeight="40"/>

            <!-- Formulaire (caché par défaut, affiché après validation du token) -->
            <VBox fx:id="formBox" spacing="15" prefWidth="350" visible="false" managed="false">

                <!-- Nouveau mot de passe -->
                <VBox spacing="5">
                    <Label text="Nouveau mot de passe" style="-fx-font-weight: bold; -fx-text-fill: #333;"/>
                    <PasswordField fx:id="newPasswordField"
                                   promptText="********"
                                   style="-fx-padding: 12; -fx-background-radius: 8; -fx-border-radius: 8;
                                          -fx-border-color: #ddd; -fx-border-width: 1;"/>
                </VBox>

                <!-- Force du mot de passe -->
                <Label fx:id="passwordStrengthLabel" style="-fx-font-size: 11px;"/>

                <!-- Confirmation -->
                <VBox spacing="5">
                    <Label text="Confirmer le mot de passe" style="-fx-font-weight: bold; -fx-text-fill: #333;"/>
                    <PasswordField fx:id="confirmPasswordField"
                                   promptText="********"
                                   style="-fx-padding: 12; -fx-background-radius: 8; -fx-border-radius: 8;
                                          -fx-border-color: #ddd; -fx-border-width: 1;"/>
                </VBox>

                <!-- Règles du mot de passe -->
                <VBox spacing="3" style="-fx-padding: 10 0 0 0;">
                    <Label text="📌 Le mot de passe doit contenir :"
                           style="-fx-font-weight: bold; -fx-text-fill: #555; -fx-font-size: 11px;"/>
                    <Label text="• Au moins 6 caractères"
                           style="-fx-text-fill: #777; -fx-font-size: 11px;"/>
                    <Label text="• Au moins une majuscule"
                           style="-fx-text-fill: #777; -fx-font-size: 11px;"/>
                    <Label text="• Au moins une minuscule"
                           style="-fx-text-fill: #777; -fx-font-size: 11px;"/>
                    <Label text="• Au moins un chiffre"
                           style="-fx-text-fill: #777; -fx-font-size: 11px;"/>
                </VBox>
            </VBox>

            <!-- Label de statut -->
            <Label fx:id="statusLabel" style="-fx-font-size: 13px;" wrapText="true"/>

            <!-- Boutons -->
            <HBox alignment="CENTER" spacing="15" prefWidth="350">
                <Button fx:id="resetPasswordBtn"
                        text="Réinitialiser"
                        onAction="#handleResetPassword"
                        style="-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-font-weight: bold;
                               -fx-padding: 12 20; -fx-background-radius: 8; -fx-cursor: hand;"
                        prefWidth="160"
                        disable="true"/>

                <Button fx:id="backToLoginBtn"
                        text="Retour au login"
                        onAction="#handleBackToLogin"
                        style="-fx-background-color: #6c757d; -fx-text-fill: white; -fx-font-weight: bold;
                               -fx-padding: 12 20; -fx-background-radius: 8; -fx-cursor: hand;"
                        prefWidth="160"/>
            </HBox>

            <!-- Note de sécurité -->
            <Label text="🔒 Connexion sécurisée"
                   style="-fx-text-fill: #999; -fx-font-size: 11px;"/>
        </VBox>
    </center>
</BorderPane>