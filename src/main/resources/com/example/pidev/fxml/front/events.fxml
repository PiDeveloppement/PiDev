<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.scene.image.*?>
<?import javafx.geometry.Insets?>

<VBox spacing="0" xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.pidev.controller.front.EventsFrontController"
      style="-fx-background-color: #f8fafc;">

    <!-- ==================== NAVBAR FIXE ==================== -->
    <HBox style="-fx-background-color: white; -fx-padding: 15 60; -fx-border-color: #e2e8f0; -fx-border-width: 0 0 1 0;"
          alignment="CENTER_LEFT" prefHeight="80">
        <!-- Logo -->
        <HBox spacing="10" alignment="CENTER_LEFT">
            <ImageView fitHeight="40" fitWidth="40" preserveRatio="true">
                <Image url="@../images/logo.png" />
            </ImageView>
            <Label text="EventFlow" style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: #0D47A1;"/>
        </HBox>

        <Region HBox.hgrow="ALWAYS"/>

        <!-- Liens de navigation -->
        <HBox spacing="40" alignment="CENTER">
            <Button text="Accueil" onAction="#handleGoToHome"
                    style="-fx-background-color: transparent; -fx-text-fill: #1e293b; -fx-font-size: 16px; -fx-font-weight: 500; -fx-cursor: hand;"/>
            <Button text="Événements"
                    style="-fx-background-color: transparent; -fx-text-fill: #0D47A1; -fx-font-size: 16px; -fx-font-weight: 700; -fx-cursor: hand; -fx-underline: true;"/>
            <Button text="Contact" onAction="#handleGoToContact"
                    style="-fx-background-color: transparent; -fx-text-fill: #1e293b; -fx-font-size: 16px; -fx-font-weight: 500; -fx-cursor: hand;"/>
        </HBox>

        <Region HBox.hgrow="ALWAYS"/>

        <!-- Boutons d'action -->
        <HBox spacing="15">
            <Button text="Connexion" onAction="#handleLogin"
                    style="-fx-background-color: transparent; -fx-text-fill: #0D47A1; -fx-font-weight: bold;
                           -fx-border-color: #0D47A1; -fx-border-width: 1.5; -fx-border-radius: 8;
                           -fx-padding: 8 24; -fx-cursor: hand;"/>
            <Button text="Inscription" onAction="#handleSignup"
                    style="-fx-background-color: #0D47A1; -fx-text-fill: white; -fx-font-weight: bold;
                           -fx-background-radius: 8; -fx-padding: 8 24; -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(13,71,161,0.3), 8, 0, 0, 2);"/>
        </HBox>
    </HBox>

    <!-- ==================== CONTENU PRINCIPAL ==================== -->
    <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS"
                style="-fx-background-color: transparent; -fx-border-color: transparent;"
                hbarPolicy="NEVER" vbarPolicy="AS_NEEDED">

        <VBox spacing="40" style="-fx-padding: 60 100;">

            <!-- HEADER -->
            <VBox spacing="15" alignment="CENTER">
                <Label text="Tous les événements"
                       style="-fx-font-size: 42px; -fx-font-weight: bold; -fx-text-fill: #0A1929;"/>
                <Label text="Découvrez et participez aux événements qui vous intéressent"
                       style="-fx-font-size: 18px; -fx-text-fill: #64748b;"/>
            </VBox>

            <!-- BARRE DE RECHERCHE ET FILTRES -->
            <VBox spacing="20" style="-fx-background-color: white; -fx-padding: 30; -fx-background-radius: 16;
                                     -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 10, 0, 0, 2);">
                <!-- Recherche -->
                <HBox spacing="15" alignment="CENTER_LEFT">
                    <Label text="🔍" style="-fx-font-size: 20px;"/>
                    <TextField fx:id="searchField" promptText="Rechercher un événement par titre, lieu..."
                               style="-fx-background-color: #f8fafc; -fx-background-radius: 10; -fx-padding: 12 20;
                                      -fx-border-color: #e2e8f0; -fx-border-width: 1; -fx-border-radius: 10;
                                      -fx-font-size: 15px; -fx-pref-width: 500;"/>
                    <Button text="Rechercher" onAction="#handleSearch"
                            style="-fx-background-color: #0D47A1; -fx-text-fill: white; -fx-font-weight: bold;
                                   -fx-background-radius: 10; -fx-padding: 12 30; -fx-cursor: hand;"/>
                </HBox>

                <!-- Filtres -->
                <HBox spacing="20" alignment="CENTER_LEFT">
                    <Label text="Filtrer par:" style="-fx-font-size: 15px; -fx-font-weight: 600; -fx-text-fill: #475569;"/>

                    <ComboBox fx:id="categoryFilter" promptText="📂 Catégorie" prefWidth="200"
                              style="-fx-background-color: #f8fafc; -fx-background-radius: 8; -fx-padding: 8 15;
                                     -fx-border-color: #cbd5e1; -fx-border-width: 1; -fx-border-radius: 8;"/>

                    <ComboBox fx:id="dateFilter" promptText="📅 Date" prefWidth="180"
                              style="-fx-background-color: #f8fafc; -fx-background-radius: 8; -fx-padding: 8 15;
                                     -fx-border-color: #cbd5e1; -fx-border-width: 1; -fx-border-radius: 8;"/>

                    <ComboBox fx:id="priceFilter" promptText="💰 Prix" prefWidth="150"
                              style="-fx-background-color: #f8fafc; -fx-background-radius: 8; -fx-padding: 8 15;
                                     -fx-border-color: #cbd5e1; -fx-border-width: 1; -fx-border-radius: 8;"/>

                    <Button text="🔄 Réinitialiser" onAction="#handleResetFilters"
                            style="-fx-background-color: transparent; -fx-text-fill: #64748b; -fx-cursor: hand;
                                   -fx-border-color: #cbd5e1; -fx-border-width: 1; -fx-border-radius: 8; -fx-padding: 8 20;"/>
                </HBox>
            </VBox>

            <!-- COMPTEUR DE RÉSULTATS -->
            <HBox spacing="10" alignment="CENTER_LEFT">
                <Label fx:id="resultCountLabel" text="0 événement(s) trouvé(s)"
                       style="-fx-font-size: 16px; -fx-text-fill: #475569; -fx-font-weight: 500;"/>
            </HBox>

            <!-- GRILLE D'ÉVÉNEMENTS (3 colonnes) -->
            <FlowPane fx:id="eventsGrid" hgap="30" vgap="30" prefWrapLength="1200">
                <!-- Les cartes d'événements seront ajoutées dynamiquement ici -->
            </FlowPane>

            <!-- MESSAGE SI AUCUN RÉSULTAT -->
            <VBox fx:id="noResultsMessage" spacing="20" alignment="CENTER"
                  style="-fx-padding: 60 0;" visible="false" managed="false">
                <Label text="😕" style="-fx-font-size: 60px;"/>
                <Label text="Aucun événement trouvé"
                       style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #475569;"/>
                <Label text="Essayez de modifier vos critères de recherche"
                       style="-fx-font-size: 16px; -fx-text-fill: #64748b;"/>
            </VBox>

            <!-- FOOTER -->
            <VBox spacing="15" alignment="CENTER" style="-fx-padding: 80 0 40 0; -fx-border-color: #e2e8f0; -fx-border-width: 1 0 0 0;">
                <Label text="© 2026 EventFlow - Tous droits réservés"
                       style="-fx-text-fill: #94a3b8; -fx-font-size: 14px;"/>
                <HBox spacing="30" alignment="CENTER">
                    <Button text="Conditions d'utilisation" style="-fx-background-color: transparent; -fx-text-fill: #64748b; -fx-cursor: hand;"/>
                    <Button text="Politique de confidentialité" style="-fx-background-color: transparent; -fx-text-fill: #64748b; -fx-cursor: hand;"/>
                    <Button text="Contact" style="-fx-background-color: transparent; -fx-text-fill: #64748b; -fx-cursor: hand;"/>
                </HBox>
            </VBox>

        </VBox>
    </ScrollPane>
</VBox>

