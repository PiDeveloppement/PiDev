<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.geometry.Insets?>

<VBox spacing="20" alignment="TOP_CENTER" style="-fx-background-color: white; -fx-background-radius: 15;"
      xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.pidev.controller.resource.SalleFormController">
    <padding><Insets top="30" right="50" bottom="30" left="50"/></padding>

    <Label fx:id="titleLabel" text="Ajouter une Salle" style="-fx-font-size: 24; -fx-font-weight: bold; -fx-text-fill: #1e293b;"/>

    <VBox spacing="15" maxWidth="400">
        <TextField fx:id="nameField" promptText="Nom de la salle" style="-fx-pref-height: 40;"/>
        <TextField fx:id="capacityField" promptText="Capacité (ex: 30)" style="-fx-pref-height: 40;"/>
        <TextField fx:id="buildingField" promptText="Bâtiment (ex: Bloc G)" style="-fx-pref-height: 40;"/>
        <TextField fx:id="floorField" promptText="Étage" style="-fx-pref-height: 40;"/>
        <ComboBox fx:id="statusCombo" promptText="Statut" maxWidth="Infinity" style="-fx-pref-height: 40;"/>

        <HBox spacing="10" alignment="CENTER_LEFT">
            <Button text="📷 Image" onAction="#choisirImage" style="-fx-background-color: #64748b; -fx-text-fill: white;"/>
            <ImageView fx:id="previewImage" fitHeight="80" fitWidth="80" preserveRatio="true"/>
        </HBox>
    </VBox>

    <HBox spacing="20" alignment="CENTER">
        <Button text="Annuler" onAction="#annuler" prefWidth="120" style="-fx-background-color: #ef4444; -fx-text-fill: white; -fx-font-weight: bold;"/>
        <Button text="Enregistrer" onAction="#enregistrer" prefWidth="120" style="-fx-background-color: #22c55e; -fx-text-fill: white; -fx-font-weight: bold;"/>
    </HBox>
</VBox>